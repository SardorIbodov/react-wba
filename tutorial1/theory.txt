Babel => Library that converts JSX to the format which Brauser can read

npx create-react-app appName => command for creating React app

npm start => to start a project

README.md => gives information about project

package.json => gives all information about libraries and scripts used in project

.gitignore => prevents uploading some files to git / github

npm install or npm i => setups the configurations to the computer

node_modules => folder of all libraries' configurations

When we start our project first of all index.js which is inside
of App folder, runs 

- Why we call projects that were built in React SPA(Single Page Application)?
- Because entire project has only one HTML file

React.StrictMode => prevents some problems which may occur while working with libraries


